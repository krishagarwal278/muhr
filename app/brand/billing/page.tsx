import { appPageTitleVariants } from "@/components/ui/page-header";

export default function BrandBillingPage() {
  return (
    <div>
      <h1 className={appPageTitleVariants()}>Billing</h1>
      <p className="mt-2 max-w-xl text-sm text-neutral-600">Billing for brand workspaces is not wired up yet.</p>
    </div>
  );
}
